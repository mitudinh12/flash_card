/**
 * Provides Data Access Objects (DAOs) used to interact with the database.
 *
 * <p>Each DAO class in this package is responsible for handling the persistence
 * logic (Create, Read, Update, Delete) for a specific entity in the system.
 * DAOs abstract the interaction with JPA and help maintain a clean separation
 * between business logic and database operations.</p>
 *
 * <p>This package follows the Singleton design pattern to ensure one instance
 * of each DAO is used across the application, improving performance and consistency.</p>
 */
package com.flash_card.model.dao;
