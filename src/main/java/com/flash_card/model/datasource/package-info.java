/**
 * Contains classes for managing the application's database connections.
 *
 * <p>This package provides the necessary configuration and connection setup for
 * accessing the underlying database using JPA with MariaDB.</p>
 *
 * <p>It typically includes utility classes or singleton instances that manage
 * `EntityManagerFactory` and `EntityManager` lifecycles, ensuring efficient and safe
 * interactions with the database.</p>
 */
package com.flash_card.model.datasource;
