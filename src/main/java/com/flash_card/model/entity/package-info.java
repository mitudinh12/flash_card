/**
 * Defines the entity classes representing the application's domain model.
 *
 * <p>Each class in this package is annotated with JPA (Jakarta Persistence API)
 * annotations and is mapped to a corresponding table in the MariaDB database.</p>
 *
 * <p>These entity classes are used throughout the application to interact with
 * the database in an object-oriented way, and they serve as the foundation for
 * all data access operations.</p>
 */
package com.flash_card.model.entity;
